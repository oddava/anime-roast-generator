# Faster installs
prefer-offline=true
cache-min=3600

# Reduce noise
loglevel=error

# Speed up resolution
prefer-dedupe=true

# Parallel downloads
maxsockets=10

# Use faster registry
registry=https://registry.npmjs.org/
